# Required for live Gemini scoring in the AI Report Card.
# Heuristic fallback still works without a key.
GEMINI_API_KEY=

# Optional Vite-prefixed alias (also supported)
VITE_GEMINI_API_KEY=

# Hosted app URL (optional)
APP_URL=
